- name: install - Software installieren
  apt: 
    name:
      - postfix
      - postfix-mysql
      - mariadb-server
      - automysqlbackup
      - dovecot-core
      - dovecot-imapd
      - dovecot-sieve
      - dovecot-managesieved
      - dovecot-lmtpd
      - dovecot-mysql
      - rspamd
      - clamav
      - clamsmtp
      - redis-server
      - bind9
      - roundcube
      - roundcube-mysql
      - roundcube-plugins
      - composer
      - memcached
      - php-memcache
      - php-gd
      - python3-pymysql
    install_recommends: no 
