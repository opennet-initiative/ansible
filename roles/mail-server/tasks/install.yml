- name: install - Software installieren
  apt: 
    name:
      - postfix
      - postfix-mysql
      - libsasl2-modules
      - mariadb-server
      - automysqlbackup
      - dovecot-core
      - dovecot-imapd
      - dovecot-sieve
      - dovecot-managesieved
      - dovecot-lmtpd
      - dovecot-mysql
      - rspamd
      - redis-server
      - bind9
      - clamav-daemon
      - clamav-freshclam
      - roundcube
      - roundcube-mysql
      - roundcube-plugins
      - composer
      - memcached
      - php-memcache
      - php-gd
      - python3-pymysql
      - redis-server
    install_recommends: no 
