server {
	listen 80;
	listen [::]:80;

	server_name {{ short_hostname }}.opennet-initiative.de;
	server_name {{ short_hostname }}.on-i.de;
	server_name {{ short_hostname }}.on;

	root /var/www;

	# der vpnstatus ist die typische Information jedes UGW-Knotens
	location = / {
		return 302 /vpnstatus/;
	}

	access_log off;
}
