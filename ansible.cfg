[defaults]

hostfile = hosts
roles_path = roles
remote_user = root
nocows = 1

# Zeitstempel und uid/host-Variablen fuehren leider zu unnoetigen Aenderungen ohne Relevanz
# siehe https://github.com/ansible/ansible/issues/5317
ansible_managed = ACHTUNG: verwaltet via ansible - siehe https://wiki.opennet-initiative.de/wiki/Opennet_ansible
